<%= form_for(@mailing_address) do |f| %>
  <% if @mailing_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailing_address.errors.count, "error") %> prohibited this mailing_address from being saved:</h2>

      <ul>
      <% @mailing_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
